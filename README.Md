# Online Book Store Frontend

This is the React frontend for the Online Book Store backend.

## Available Scripts

- `npm install` → Install dependencies
- `npm start` → Run frontend at http://localhost:3000
